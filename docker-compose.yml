# Commerce Lab — local stack.
# Phase 0: order-service. Phases 2 and 3A add each service's own Postgres database.
# Later phases add Kafka/Redpanda (Phase 4) and observability (Phase 5).
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: orderdb
      POSTGRES_USER: order
      POSTGRES_PASSWORD: order
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      # Named volume → orders survive `docker compose down` + restart.
      - order-pgdata:/var/lib/postgresql/data
    networks:
      - order-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U order -d orderdb"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  order-service:
    build: ./order-service
    image: commerce-lab/order-service:0.0.1
    ports:
      - "${ORDER_SERVICE_PORT:-8080}:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/orderdb
      SPRING_DATASOURCE_USERNAME: order
      SPRING_DATASOURCE_PASSWORD: order
      INVENTORY_BASE_URL: ${INVENTORY_BASE_URL:-http://inventory-service:8081}
      ORDER_RECOVERY_ENABLED: ${ORDER_RECOVERY_ENABLED:-true}
      ORDER_RECOVERY_FIXED_DELAY_MS: ${ORDER_RECOVERY_FIXED_DELAY_MS:-5000}
      ORDER_RECOVERY_BATCH_SIZE: ${ORDER_RECOVERY_BATCH_SIZE:-20}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - order-network
      - service-network
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 3s
      retries: 5
      # Allow for Flyway migration + JPA validation on a cold start.
      start_period: 40s
    restart: unless-stopped

  inventory-postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: inventory
      POSTGRES_USER: inventory
      POSTGRES_PASSWORD: inventory
    ports:
      - "${INVENTORY_POSTGRES_PORT:-5433}:5432"
    volumes:
      - inventory-pgdata:/var/lib/postgresql/data
    networks:
      - inventory-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U inventory -d inventory"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  inventory-service:
    build: ./inventory-service
    image: commerce-lab/inventory-service:0.0.1
    ports:
      - "${INVENTORY_SERVICE_PORT:-8081}:8081"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://inventory-postgres:5432/inventory
      SPRING_DATASOURCE_USERNAME: inventory
      SPRING_DATASOURCE_PASSWORD: inventory
    depends_on:
      inventory-postgres:
        condition: service_healthy
    networks:
      - inventory-network
      - service-network
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8081/actuator/health"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 40s
    restart: unless-stopped

volumes:
  order-pgdata:
  inventory-pgdata:

networks:
  order-network:
  inventory-network:
  service-network:
